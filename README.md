# Install venv and use it for pythong
# Install all the packages with `pip install -Ur requirements.txt`
# Run with pytest to get the results `pytest`
# Run tests with `pytest --alluredir allure-results`
